<div class="main">
    <div class="content-form">
        <h3>編集する</h3>
            <%= form_with(model: @info, local: true) do |form| %>
                <%= form.text_field :name, placeholder: "山田 太郎" %>
                <%= form.text_field :image, placeholder: "Image.url" %>
                 <%= form.text_area :text, placeholder: "text", row:"10" %>
                <%= form.submit "編集" %>
            <% end %>

    </div>
</div>